# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# scratch / logs locales — nunca versionar
*.log
tsc-*.txt

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Claves, certificados y archivos sensibles — NUNCA subir al repositorio
llave-aycweb
*.pfx
*.p12
*.key
*.crt
*.cer

# Internal/private app
/aycweb-panel/

# Kapso (paquete anidado): deps locales y caché del CLI — no versionar
/kapso/node_modules/
/kapso/.kapso/cache/
.env
.env.local
.env*.local
.supabase/
